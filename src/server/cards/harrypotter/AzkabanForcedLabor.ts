import {IProjectCard} from '../IProjectCard';import {Tag} from '../../../common/cards/Tag';import {Card} from '../Card';import {CardType} from '../../../common/cards/CardType';import {CardName} from '../../../common/cards/CardName';import {CardRenderer} from '../render/CardRenderer';
export class AzkabanForcedLabor extends Card implements IProjectCard {
  constructor() {super({type: CardType.AUTOMATED, name: CardName.AZKABAN_FORCED_LABOR, tags: [Tag.BUILDING, Tag.MARS], cost: 17, victoryPoints: -2, behavior: {production: {steel: 3, megacredits: 2}}, metadata: {cardNumber: 'HP87', renderData: CardRenderer.builder((b) => {b.production((pb) => pb.steel(3).megacredits(2));}), description: 'Increase steel production 3 steps and M€ production 2 steps. -2 VP. (The island fortress drains hope from every soul within its walls.)'}});}
}
