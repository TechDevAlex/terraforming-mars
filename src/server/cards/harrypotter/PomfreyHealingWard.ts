import {IProjectCard} from '../IProjectCard';import {Tag} from '../../../common/cards/Tag';import {Card} from '../Card';import {CardType} from '../../../common/cards/CardType';import {CardName} from '../../../common/cards/CardName';import {CardRenderer} from '../render/CardRenderer';
export class PomfreyHealingWard extends Card implements IProjectCard {
  constructor() {super({type: CardType.AUTOMATED, name: CardName.POMFREY_HEALING_WARD, tags: [Tag.BUILDING, Tag.SCIENCE], cost: 10, victoryPoints: 1, behavior: {production: {megacredits: 2}}, metadata: {cardNumber: 'HP111', renderData: CardRenderer.builder((b) => {b.production((pb) => pb.megacredits(2));}), description: 'Increase M€ production 2 steps. (She can mend bones in a heartbeat but cannot cure the foolishness that breaks them.)'}});}
}
