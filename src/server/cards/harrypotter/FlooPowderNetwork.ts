import {IProjectCard} from '../IProjectCard';import {Tag} from '../../../common/cards/Tag';import {Card} from '../Card';import {CardType} from '../../../common/cards/CardType';import {CardName} from '../../../common/cards/CardName';import {CardRenderer} from '../render/CardRenderer';
export class FlooPowderNetwork extends Card implements IProjectCard {
  constructor() {super({type: CardType.AUTOMATED, name: CardName.FLOO_POWDER_NETWORK, tags: [Tag.BUILDING, Tag.POWER], cost: 10, behavior: {production: {energy: 1, megacredits: 1}}, metadata: {cardNumber: 'HP169', renderData: CardRenderer.builder((b) => {b.production((pb) => pb.energy(1).megacredits(1));}), description: 'Increase energy and M€ production 1 step each. (Speak clearly and keep your elbows tucked in—Floo travel is not for the clumsy.)'}});}
}
